<%= form_for(@situacaoorcamento) do |f| %>
  <% if @situacaoorcamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@situacaoorcamento.errors.count, "error") %> prohibited this situacaoorcamento from being saved:</h2>

      <ul>
      <% @situacaoorcamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
