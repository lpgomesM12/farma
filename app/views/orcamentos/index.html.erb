  <!-- Content Header (Page header) -->
<section class="content-header">
     <!--Mensagens aqui-->
      <%if notice %>  
      <%end%>
    <!--<small></small>-->

        <%= link_to criaOrcamento_path, class: "btn btn-primary" do %>
                  <span class="glyphicon glyphicon-file"></span>Novo Orçamento
    <% end %>

  </section>  
   <!-- Main content -->
 <section class="content">

    <!-- Small boxes (Stat box) -->
       <div class="box box-primary">
             <div class="box-header with-border">
               <h3 class="box-title">Orçamentos</h3>
              </div><!-- /.box-header --> 
      <div class="box-body table-responsive no-padding">             
          <table class="table table-hover">
            <thead>
            </thead>
            <tbody>
           <%@orcamentos.each do |orcamento| %>
                <tr>
                  <td><%= link_to orcamento.nome, edit_orcamento_path(orcamento)%></td>
                <td align='right'>
                  <div class="btn-group">
                      <%= link_to orcamento, class: "btn btn-default btn-sm" do %>
                        <span class="glyphicon glyphicon-list-alt"></span> Exibir
                      <% end %>             
                  </div>
                  </td> 
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      </div><!-- /.box -->
</section>
